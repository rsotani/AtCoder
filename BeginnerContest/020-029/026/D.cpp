#include <bits/stdc++.h>
using namespace std;

int main(){
  int A, B, C;
  cin >> A >> B >> C;
  double t = 100.0/A;
  double f = A*t - B*sin((double)C*t*M_PI) - 100
  while(true){
    if (abs(f) < 1e-6) break;
    else{

    }
  }
  cout << fixed << setprediction(10);
  cout << t << endl;
}
