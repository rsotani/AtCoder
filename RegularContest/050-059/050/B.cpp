#include <bits/stdc++.h>
using namespace std;

int main(){
  long long R, B, x, y;
  cin >> R >> B >> x >> y;
  long long ans = 0;
  ans += R/x;
  R = R%x;
  B -= R/x;
  ans +=
  while (true){

  }
}
